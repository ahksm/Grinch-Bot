# 🏗️ Архитектура бота

## Общая структура

```
┌─────────────┐
│   Telegram  │
│    User     │
└──────┬──────┘
       │
       │ /start or message
       ▼
┌─────────────────────────────┐
│  TelegramBot (node-telegram)│
│        bot-api)              │
└──────────┬──────────────────┘
           │
           ▼
    ┌──────────────┐
    │ State Machine│
    └──────┬───────┘
           │
           ├─► START → Приветствие + кнопка
           ├─► TASK_1 → Задание "Несмеяна"
           ├─► TASK_2 → Задание "Оратор"
           ├─► TASK_3 → Задание "Елка"
           ├─► TASK_4 → Задание "Танец"
           ├─► TASK_5 → Финал (ожидание)
           └─► COMPLETED → Квест завершен
```

## Компоненты системы

### 1. State Machine (Машина состояний)

Управляет переходами между этапами квеста:

```javascript
const STATES = {
  START: 'start',
  TASK_1: 'task_1',
  TASK_2: 'task_2',
  TASK_3: 'task_3',
  TASK_4: 'task_4',
  TASK_5: 'task_5',
  COMPLETED: 'completed'
};
```

### 2. User State Storage

Хранит текущее состояние каждого пользователя:

```javascript
userStates = {
  [userId]: {
    state: 'task_1' // текущий этап
  }
}
```

**Особенности:**
- Хранится в оперативной памяти
- Сбрасывается при перезапуске бота
- Каждый пользователь имеет независимое состояние

### 3. Video Manager

Управляет загрузкой и отправкой видео:

```javascript
function getVideoPath(number) {
  // Ищет файл вида: number_*.mp4 в папке videos/
  // Возвращает полный путь к файлу
}

function sendVideo(chatId, videoNumber, options) {
  // Отправляет видео пользователю
  // Обрабатывает ошибки
}
```

### 4. Message Handler

Обрабатывает входящие сообщения:

```
┌──────────────┐
│   Message    │
└──────┬───────┘
       │
       ├─► Команда /start?
       │   └─► Сброс состояния, отправка приветствия
       │
       ├─► Callback query (кнопка)?
       │   └─► Старт квеста
       │
       ├─► Текстовое сообщение?
       │   └─► Отправка случайной фразы Гринча
       │
       └─► Видео/Фото?
           └─► Переход к следующему этапу
```

## Логика переходов

### Успешный путь (Happy Path)

```
/start
  ↓
Приветствие (видео 0) + кнопка
  ↓
[Кнопка] "Начать поиск багов"
  ↓
Задание 1 (видео 1)
  ↓
[Видео/Фото от пользователя]
  ↓
Задание 2 (видео 2)
  ↓
[Видео/Фото от пользователя]
  ↓
Задание 3 (видео 3)
  ↓
[Видео/Фото от пользователя]
  ↓
Задание 4 (видео 4)
  ↓
[Видео/Фото от пользователя]
  ↓
Финал 1 (видео 5)
  ↓
[Видео/Фото от пользователя]
  ↓
Переход (видео 6)
  ↓
Текст: "Гринч покинул(а) чат..."
  ↓
Финал 2 (видео 7)
  ↓
COMPLETED
```

### Обработка ошибок

```
[Текст от пользователя]
  ↓
Выбор случайной фразы из ERROR_MESSAGES[]
  ↓
Отправка фразы
  ↓
Ожидание медиа (состояние не меняется)
```

## Функции и их назначение

| Функция | Назначение |
|---------|-----------|
| `initUserState(userId)` | Инициализирует состояние пользователя |
| `getVideoPath(number)` | Находит путь к видео по номеру |
| `sendVideo(chatId, number, options)` | Отправляет видео пользователю |
| `getRandomErrorMessage()` | Возвращает случайную фразу об ошибке |
| `simulateViewing()` | Пауза 2 сек (имитация просмотра) |

## Обработчики событий

| Событие | Обработчик | Действие |
|---------|-----------|----------|
| `/start` | `bot.onText(/\/start/)` | Сброс состояния, отправка приветствия |
| Callback query | `bot.on('callback_query')` | Начало квеста |
| Message | `bot.on('message')` | Проверка типа сообщения, переход по state machine |
| Polling error | `bot.on('polling_error')` | Логирование ошибок |

## Особенности реализации

### Асинхронность
Все операции с Telegram API выполняются асинхронно (async/await)

### Имитация просмотра
После получения видео бот делает паузу 2 секунды, чтобы создать ощущение, что Гринч смотрит видео

### Независимость пользователей
Каждый пользователь имеет собственное состояние, квесты не пересекаются

### Гибкость формата видео
Принимаются:
- Обычное видео (`msg.video`)
- Видео-кружочки (`msg.video_note`)
- Фото (`msg.photo`)

### Обработка отсутствующих файлов
Если видео не найдено, бот:
1. Логирует ошибку в консоль
2. Отправляет пользователю сообщение с инструкцией
3. Не переходит к следующему этапу

## Масштабирование

### Текущие ограничения
- Состояния хранятся в памяти (теряются при перезапуске)
- Нет персистентного хранилища

### Возможные улучшения
1. **База данных**: Использовать SQLite/PostgreSQL/MongoDB для хранения состояний
2. **Кэширование**: Кэшировать загруженные видео на стороне Telegram
3. **Логирование**: Добавить полноценное логирование действий пользователей
4. **Метрики**: Отслеживать прохождение квеста (где пользователи застревают)
5. **Админ-панель**: Интерфейс для мониторинга активных квестов

## Безопасность

### Текущие меры
- Токен бота жестко закодирован (для простоты)
- Нет аутентификации пользователей
- Публичный доступ для всех

### Рекомендации для продакшена
1. Хранить токен в переменных окружения (.env)
2. Добавить whitelist пользователей (только Глеб)
3. Ограничить rate limiting (защита от спама)
