# 📖 Руководство для заказчика

## 🎬 Как менять видеоролики

### Структура видео:

В папке `videos/` должны быть 7 видеофайлов с названиями:

```
videos/
  ├── 0_название.mp4  - Приветственное видео
  ├── 1_название.mp4  - Задание "Несмеяна"
  ├── 2_название.mp4  - Задание "Оратор"
  ├── 3_название.mp4  - Задание "Елка"
  ├── 4_название.mp4  - Задание "Танец"
  ├── 5_название.mp4  - Финальное задание от Гринча
  └── 6_название.mp4  - Видео с подарком от Санты
```

### ⚠️ ВАЖНО:

1. **Название файла ДОЛЖНО начинаться с номера и подчеркивания:**
   - ✅ Правильно: `0_приветствие.mp4`, `1_задание1.mp4`, `5_финал.mp4`
   - ❌ Неправильно: `приветствие.mp4`, `задание_1.mp4`, `5-финал.mp4`

2. **После подчеркивания может быть любое название** (на русском, английском, любые буквы)

3. **Расширение:** `.mp4` (рекомендуется), `.avi`, `.mov` и другие видео-форматы

### 🔄 Как заменить видео:

#### Локально (на компьютере):
1. Откройте папку `videos/`
2. Удалите старый файл (например, `0_приветствие.mp4`)
3. Добавьте новый файл с **тем же номером** (например, `0_новое_приветствие.mp4`)
4. Перезапустите бота: `npm start`

#### На Render.com (в продакшене):
1. Замените файл в папке `videos/` на компьютере
2. Загрузите на GitHub:
   ```bash
   cd "E:\Workspace\secret santa bot"
   git add videos/
   git commit -m "Обновлены видео"
   git push
   ```
3. Render автоматически обновит бота через 2-3 минуты

### 💡 Советы:

- **Сжимайте видео** для быстрой отправки (рекомендуется до 10MB на файл)
- Можно **заменять только нужные видео**, не обязательно все сразу
- Можно менять название после подчеркивания как угодно: `0_зима.mp4`, `0_лето.mp4`, `0_test.mp4`

---

## 📬 Как получать видео от пользователей

Бот автоматически пересылает все видео от пользователей заказчику в Telegram!

### Настройка:

#### 1. Узнайте свой Telegram ID:
1. Откройте Telegram
2. Найдите бота [@userinfobot](https://t.me/userinfobot)
3. Нажмите "Start"
4. Бот пришлет вам ваш ID (например: `123456789`)

#### 2. Добавьте ID в настройки Render:

1. Откройте ваш сервис на [Render.com](https://render.com)
2. Перейдите на вкладку **"Environment"**
3. Нажмите **"Add Environment Variable"**
4. Добавьте переменную:
   - **Key**: `ADMIN_CHAT_ID`
   - **Value**: ваш Telegram ID (например: `123456789`)
5. Нажмите **"Save Changes"**
6. Сервис автоматически перезапустится

#### 3. Готово!

Теперь когда пользователь отправляет видео боту, вы автоматически получите:
- 📹 Само видео (или кружочек)
- 👤 Информацию о пользователе (имя или @username)
- 🎬 Этап квеста, на котором находится пользователь

### Пример сообщения, которое вы получите:

```
[Видео]
👤 От: @username (ID: 987654321)
🎬 Этап: task_2
```

---

## 🔧 Дополнительные возможности

### Отключить пересылку видео:

Если не нужно получать видео, просто удалите переменную `ADMIN_CHAT_ID` из Environment на Render.

### Изменить тексты сообщений:

Откройте файл `src/bot.js` и найдите:
- `ERROR_MESSAGES` - сообщения при отправке текста (строка ~38)
- `WATCHING_MESSAGES` - сообщения о просмотре видео (строка ~53)
- `WAITING_MESSAGES` - сообщения ожидания отчета (строка ~63)

Измените тексты, сохраните и загрузите на GitHub (`git commit` → `git push`)

---

## ❓ Часто задаваемые вопросы

### Q: Можно ли добавить больше заданий (больше 7 видео)?
A: Да, но потребуется изменить код. Напишите разработчику.

### Q: Можно ли сделать несколько вариантов квеста?
A: Да, можно сделать чтобы бот случайно выбирал из нескольких вариантов видео. Напишите разработчику.

### Q: Можно ли принимать фото обратно?
A: Да, нужно изменить одну строку в коде. Напишите разработчику.

### Q: Бот не отправляет видео, что делать?
A: Проверьте что в папке `videos/` есть файлы с правильными названиями (`0_*.mp4`, `1_*.mp4` и т.д.)

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи на Render (вкладка "Logs")
2. Убедитесь, что видео названы правильно
3. Проверьте что переменные окружения установлены
4. Свяжитесь с разработчиком
