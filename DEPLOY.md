# 🚀 Деплой на Render.com

## Подготовка

### 1. Создайте аккаунт на GitHub (если еще нет)
Перейдите на https://github.com и зарегистрируйтесь

### 2. Загрузите проект на GitHub

#### Через GitHub Desktop (проще):
1. Скачайте GitHub Desktop: https://desktop.github.com/
2. Откройте GitHub Desktop
3. File → Add Local Repository → выберите папку с ботом
4. Нажмите "Publish repository"
5. Снимите галочку "Keep this code private" (если хотите публичный репозиторий)
6. Нажмите "Publish repository"

#### Через командную строку:
```bash
cd "E:\Workspace\secret santa bot"
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ВАШ_USERNAME/secret-santa-bot.git
git push -u origin main
```

## Деплой на Render.com

### 1. Создайте аккаунт на Render
- Перейдите на https://render.com
- Нажмите "Get Started for Free"
- Войдите через GitHub (удобнее)

### 2. Создайте новый сервис
1. На главной странице Render нажмите "New +"
2. Выберите "Web Service"
3. Подключите ваш GitHub репозиторий с ботом
4. Нажмите "Connect"

### 3. Настройте сервис

**Основные настройки:**
- **Name**: `grinch-quest-bot` (или любое другое имя)
- **Region**: `Frankfurt (EU Central)` (ближайший к России)
- **Branch**: `main`
- **Root Directory**: оставьте пустым
- **Runtime**: `Node`
- **Build Command**: `npm install`
- **Start Command**: `npm start`

**План:**
- Выберите **Free** (бесплатный)

### 4. Добавьте переменные окружения

В разделе "Environment Variables" добавьте:

**Key**: `TELEGRAM_BOT_TOKEN`
**Value**: `7970494384:AAGK7b0yPDFocAoG4Mb0zA6kZvCmApBmNYU`

### 5. Деплой

1. Нажмите "Create Web Service"
2. Render начнет автоматический деплой (займет 2-5 минут)
3. Дождитесь статуса "Live" в логах

## Проверка

1. Откройте вашего бота в Telegram
2. Отправьте `/start`
3. Бот должен ответить приветственным видео

## Важные моменты

### ⚠️ Ограничения бесплатного плана:
- Бот "засыпает" после 15 минут неактивности
- При первом сообщении после "сна" бот проснется (задержка 30-60 секунд)
- 750 часов работы в месяц (достаточно для личного использования)

### 💡 Что происходит при "засыпании":
- Пользователь отправляет `/start`
- Render "будит" бота (30-60 секунд)
- Бот отвечает
- После этого работает нормально, пока активен

### 🔄 Автоматическое обновление:
- Каждый `git push` в GitHub автоматически обновит бота на Render
- Изменения применятся через 2-3 минуты

## Мониторинг

### Логи:
В панели Render:
- Вкладка "Logs" - все логи бота в реальном времени
- Вкладка "Metrics" - использование CPU и памяти

### Если бот не работает:
1. Проверьте логи в Render
2. Убедитесь, что все видео загружены в репозиторий
3. Проверьте переменную окружения `TELEGRAM_BOT_TOKEN`

## Альтернатива - держать бота "бодрым"

Если не хотите, чтобы бот засыпал, используйте сервис пинга:
1. https://uptimerobot.com (бесплатный)
2. Создайте HTTP(S) монитор для вашего URL на Render
3. Интервал проверки: каждые 5 минут

Но для квеста на одного человека это не критично!

## Поддержка

Если что-то пошло не так:
- Документация Render: https://render.com/docs
- Логи всегда покажут, где ошибка
